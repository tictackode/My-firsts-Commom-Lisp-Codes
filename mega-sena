
(defun sorteio() (+ 1 ( random 59)))

; nao funcionou
(defun megasena() (let aux 6)
  (cond (> aux 0)
        (+ 1 (ramdom 59))
        (megasena())))

    ; funcionou!    
(defun mega-sena()(list(sorteio)(sorteio)(sorteio)(sorteio)(sorteio)(sorteio)))